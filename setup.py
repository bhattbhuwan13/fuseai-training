from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='a repo that contains my deliverables for all the task done during the training',
    author='bhuwan',
    license='',
)
